// The finishing-order rules live in shared, so the crest written server-side
// and the bracket drawn here can never disagree about who won. Re-exported from
// this path because the feature's components import it from here.
export {
  finalPlacements,
  placeSpans,
  type PlaceSpan,
  type PlacementStage,
} from "@unicum.gg/shared";
